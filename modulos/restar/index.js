
function restar ( a , b )
        //Se guardan dos valores dentro de la funcion restar
{

    let resultado = a - b ;
        //Los dos valores se calculan y se guarda el resultado dentro de la variable resultado
    return console.log("la resta es: " + resultado );
        //Se retorna por consola un mensaje mas el resultado ya calculado
}

module.exports = restar ;
        //Exportacion del modulo